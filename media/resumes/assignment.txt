select loc,sal from emp,dept
where dept.deptno=emp.deptno and sal=(
select max(sal) from emp where sal<(
select max(sal) from emp));

select dname,hiredate from emp,dept 
where dept.deptno=emp.deptno and hiredate=(select max(hiredate) from emp);

select * from emp FULL OUTER JOIN dept
on emp.deptno=dept.deptno;

select ename,dname from emp,dept
where emp.deptno=dept.deptno and sal<(select avg(sal) from emp where deptno=30);

 select e1.ename,e2.sal
 from emp e1,emp e2
 where e1.mgr=e2.empno and (e1.ename like 'S%' or e1.ename like 'A%') and e2.sal>(
 select sal from emp where ename='FORD');

select ename,job,loc,sal*12 as "AnnualSal",e.deptno,dname
from emp e,dept d
where e.deptno=d.deptno and sal*12=30000 and job not like 'CLERK';

 select emp.*,sal*12,deptno
 from emp
 where deptno in (select deptno
 from emp where deptno in(10,30)
 group by deptno
 having count(*)>=2);

 select dname,loc
 from dept
 where deptno=(select deptno from emp where empno=(
 select mgr from emp where ename='SMITH'));

select loc,ename
from emp,dept
where emp.deptno=dept.deptno and empno in (select mgr from emp where empno in (
select mgr from emp where job like 'SALESMAN'))

select count(*) from emp
where sal<(select sal from emp where empno in (select mgr from emp
where ename in 'BLAKE'))